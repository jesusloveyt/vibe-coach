*이 문서는 대화 예시입니다. 실제 파일 경로와 코드의 줄 위치는 사용자 환경 및 생성된 코드에 따라 달라집니다. 이 안내는 실제 채팅 내용에 포함되지 않습니다.*

# 계산기 실습 안내

**사용자**

> 나는 코딩 초보자야. solutions\calculator\AGENTS.md 를 읽고 계산기 실습을 안내해 줘. 일부 코드 편집 작업에 참여하고자 한다.

**AI**

> `solutions\calculator\AGENTS.md` 파일을 확인하고, 직접 코드를 조금씩 수정하며 배울 수 있도록 안내할게요.
>
> 계산기 파일을 준비했습니다. 계산 동작은 코드로 확인했고, 브라우저 화면은 아직 확인하지 않았습니다. **이번에는 첫 버튼 하나만 바꿔 보세요.**
>
> 1. VS Code에서 `codes/calculator.html`을 여세요. `숫자 표시 편집 시작` 주석 바로 아래에서 숫자 7 버튼을 찾으세요.
> 2. 다음 줄의 `>?</button>` 부분에서 **`?`만 `7`로** 바꾸세요. 따옴표나 `data-digit`은 그대로 두세요.
>
>    변경 전:
>
>    ```html
>    <button type="button" data-digit="7" aria-label="숫자 7">?</button>
>    ```
>
>    변경 후:
>
>    ```html
>    <button type="button" data-digit="7" aria-label="숫자 7">7</button>
>    ```
>
> 3. **Ctrl+S**로 저장하세요. 파일 탐색기에서 `codes/calculator.html`을 더블 클릭하면 브라우저로 열립니다. 이미 열었다면 **F5**로 새로고침하세요.
>
> 첫 숫자 버튼에 **7**이 보이면 “7이 보여요”라고 알려 주세요. 나머지 `?`는 다음에 함께 채우겠습니다.

**사용자**

> 7이 보여요

**AI**

> 이제 저장된 파일에서 나머지 숫자를 바꿀 위치를 확인하겠습니다.
>
> 저장된 파일에서도 첫 버튼이 `7`로 바뀐 것을 확인했습니다.
>
> 이제 `calculator.html`에서 같은 방법으로 **나머지 버튼의 `?`만** 아래 숫자로 바꿔 주세요.
>
> | 찾을 문구 | 넣을 숫자 |
> | --- | --- |
> | `data-digit="8"` | 8 |
> | `data-digit="9"` | 9 |
> | `data-digit="4"` | 4 |
> | `data-digit="5"` | 5 |
> | `data-digit="6"` | 6 |
> | `data-digit="1"` | 1 |
> | `data-digit="2"` | 2 |
> | `data-digit="3"` | 3 |
> | `data-digit="0"` | 0 |
>
> 따옴표와 `data-digit`은 그대로 두고, `>`와 `<` 사이의 `?`만 수정하세요.
>
> 모두 채우면 **Ctrl+S**로 저장하고 “검토 요청”이라고 말씀해 주세요.

**사용자**

> 검토 요청

**AI**

> 검토를 통과했습니다. 숫자 버튼 10개가 모두 올바르게 채워졌고, 버튼 구조와 계산 연결도 잘 유지되어 있습니다.
>
> 이제 브라우저에서 **F5**로 새로고침한 뒤 다음 순서로 눌러 보세요.
>
> **초기화 → 2 → + → 3 → =**
>
> 결과가 **5**로 나오는지 알려 주세요.

**사용자**

> 잘 나온다.

**AI**

> 직접 숫자를 수정하고 저장한 뒤, 검토와 계산 확인까지 마치셨습니다.
>
> 이제 계산기의 화면이나 기능에서 바꾸고 싶은 부분이 있나요? 예를 들어 버튼 크기나 색상을 바꿀 수 있습니다.

**사용자**

> 숫자는 연한 배경색을, = 버튼은 강한 청색, 초기화는 경고성 배경색으로 변경해 줘.

**AI**

> `calculator.html`을 수정했습니다.
>
> - 숫자: 연한 하늘색
> - `=`: 진한 청색과 흰 글자
> - 초기화: 연한 붉은색과 진한 빨간 글자
>
> 브라우저에서 **F5**를 눌러 확인해 보세요.
>
> 다음으로 계산 로직을 직접 수정하는 연습을 진행하시겠습니까? 완성본은 별도로 보관하고, 계산 부분을 하나씩 직접 완성하는 과정입니다.

**사용자**

> 직접 수정 연습하고 싶다.

**AI**

> 완성본은 `calculator-complete.html`에 보관했습니다. 연습 파일은 네 연산이 “아직 구현하지 않은 연산입니다”라고 표시하도록 준비했고, 초기화 동작도 코드 실행으로 확인했습니다.
>
> **먼저 덧셈 한 줄을 바꿔 보세요.**
>
> `calculator.html`을 여세요. `function add(a, b)` 바로 아래입니다.
>
> 변경 전:
>
> ```js
> return null;
> ```
>
> 변경 후:
>
> ```js
> return a + b;
> ```
>
> `a`와 `b`는 입력한 두 숫자이고, `+`는 더하기입니다. `return`은 계산한 결과를 돌려준다는 뜻입니다.
>
> **Ctrl+S**로 저장하고 브라우저에서 **F5**를 누른 뒤, **초기화 → 2 → + → 3 → =**를 눌러 보세요. 결과가 `5`로 나오면 알려 주세요.

**사용자**

> 잘 나온다. 그런데 2 + 3 = 이라는 과정식이 결과 창 위에 있었으면 좋겠다.

**AI**

> `calculator.html`에 계산 과정 표시를 추가했습니다. **F5**로 새로고침하면 결과 위에 `2 + 3 =`가 표시됩니다. 초기화를 누르면 과정식도 지워집니다. 코드 실행으로 확인했습니다.
>
> 이어서 **뺄셈 한 줄**을 수정해 보세요. `function subtract(a, b)` 아래의 반환문입니다.
>
> ```js
> return null;
> ```
>
> 이 줄을 다음처럼 바꾸세요.
>
> ```js
> return a - b;
> ```
>
> `a`에서 `b`를 빼므로 순서가 중요합니다. **Ctrl+S → F5** 후 '초기화' → '9' → '−' → '4' → '=' 를 눌러 결과가 `5`인지 알려 주세요.

**사용자**

> 반영했다.

**AI**

> 뺄셈을 올바르게 수정하셨습니다. 코드 실행으로 `9 − 4 = 5`와 과정식 표시를 확인했습니다.
>
> 이제 `calculator.html`의 **곱셈과 나눗셈을 직접 완성해 보세요.**
>
> | 수정 위치 | 바꿀 줄 | 사용할 기호 |
> | --- | --- | --- |
> | `multiply(a, b)` | `return null;` | 곱하기 `*` |
> | `divide(a, b)` | `return null;` | 나누기 `/` |
>
> 앞서 작성한 덧셈·뺄셈을 참고해 `null`을 계산식으로 바꾸세요.
>
> **Ctrl+S → F5** 후, 매번 초기화를 누르고 `6 × 7 = 42`, `8 ÷ 2 = 4`인지 확인해 보세요. 끝나면 **“로직 검토 요청”**이라고 말씀해 주세요.

**사용자**

> 로직 검토 요청

**AI**

> 나눗셈의 `return a / b;`는 올바르게 작성하셨습니다. 곱셈에서 기호 하나를 고치면 됩니다.
>
> `calculator.html`에 있는 `return a X b;`에서 **`X`를 별표 `*`로** 바꿔 주세요. JavaScript에서는 곱하기에 `*`를 사용합니다. 현재 `X` 때문에 계산 코드 전체가 실행되지 않는 상태입니다.
>
> 수정 후 **Ctrl+S → F5**로 저장하고 새로고침한 뒤, “다시 검토해 줘”라고 말씀해 주세요.

**사용자**

> 수정하고 테스트 하니 잘 되었다.
>
> 수정하신 곱셈·나눗셈도 올바릅니다. 사칙 연산과 `3 ÷ 2 = 1.5`까지 검토를 통과해 **로직 실습을 완료하셨습니다.**
>
> 혹시 위 문구와 다른 오류가 나왔다면 표시된 내용을 알려 주세요.

**사용자**

> 모두 잘 진행되었다.

**AI**

> 계산기 실습을 완료하셨습니다. 숫자 표시와 사칙 연산 코드를 직접 수정하고, 저장 → 검토 → 실행 확인까지 경험하셨습니다.
>
> 완성한 파일은 `codes/calculator.html`입니다. 파일 탐색기에서 더블 클릭하면 다시 사용할 수 있습니다. 버튼 색상과 계산 과정 표시도 반영되어 있습니다.
>
> 여기서 마치셔도 좋습니다. 다음에 이어서 하실 때는 원하는 변경 사항을 말씀해 주세요.

**사용자**

> 마무리

**AI**

> 실습을 마무리하겠습니다.
>
> 숫자 버튼과 사칙 연산을 직접 수정하고, 저장·검토·실행 확인까지 완료하셨습니다. 버튼 색상과 계산 과정 표시도 추가했습니다.
>
> 완성 파일: `codes/calculator.html`
> 파일 탐색기에서 더블 클릭하면 다시 열 수 있습니다.
